version: '1.0'

steps:
  build-prj:
    type: build
    description: UC - build step
    image-name: codefreshio/yaml-example
    dockerfile: Dockerfile
    tag: ${{CF_BRANCH}}

  unit-test:
    image: node:latest
    commands:
        - >-
         echo very very long command
        continuation of this command
        - echo block is finished