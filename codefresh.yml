version: '1.0'

steps:
  build-prj:
    type: build
    description: UC - build step
    image-name: codefreshio/yaml-example
    dockerfile: Dockerfile
    tag: ${{CF_BRANCH}}
    when:
      - condition: "'${{CF_BRANCH}}' == 'condition-step'"
      - condition: "'${{CF_BRANCH}}' == 'condition-step'"

  freestyle-step:
    image: node:latest
    working-directory : ${{initial-clone}}
    commands:
        - npm -v
    when:
      - condition: "${{CF_BRANCH}}=='conditon-step'"
      - name: "Branch is test_me1"
        condition: "'${{CF_BRANCH}}' == 'conditon-step'"
      - name: "Branch is test_me2"
        condition: "'${{CF_BRANCH}}' == 'conditon-step1'"