version: '1.0'

steps:
  build-step:
    type: build
    description: UC - build step
    image-name: codefreshio/yaml-example
    dockerfile: Dockerfile
    tag: ${{CF_BRANCH}}

  inline-composition:
      type: launch-composition
      environmentName: ${{CF_SHORT_REVISION}}
      composition:
        version: '2'
        services:
          service-name:
            image: ${{build-step1}}
            ports:
             - 3000